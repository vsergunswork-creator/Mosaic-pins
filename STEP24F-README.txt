Mosaic Pins step24f — Browser / bookmark / home-screen brand icon

Base: Mosaic-pins-main(20260823-195110).zip

Changes:
- Uses the already-approved Mosaic Pins circular brand mark as the site favicon.
- Adds favicon.ico and 32x32 PNG for browser tabs/bookmarks.
- Adds 180x180 Apple touch icon.
- Adds 192x192 and 512x512 manifest icons plus site.webmanifest.
- Connects the icon set in the <head> of every HTML page, including success/cancel.
- No page layout, checkout, payments, account/reviews backend, DHL or transactional-email logic changed.

Note: Browsers cache favicons aggressively. After deployment, a hard refresh or reopening the tab may be required before the new icon appears.
