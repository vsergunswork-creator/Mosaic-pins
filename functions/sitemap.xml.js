// Sitemap generated automatically from Airtable-backed catalog.
import { getProductsCatalog } from "./api/_airtable-products.js";
export async function onRequestGet({env,request}){try{const origin=new URL(request.url).origin;const staticPages=[`${origin}/`,`${origin}/about`,`${origin}/shipping`,`${origin}/returns`,`${origin}/reviews`,`${origin}/privacy.html`,`${origin}/impressum.html`];const {products}=await getProductsCatalog(env);const productPages=products.filter(p=>p.active).map(p=>`${origin}/p/${encodeURIComponent(p.pin)}`);const now=new Date().toISOString();const all=[...staticPages,...productPages];const body=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${all.map(loc=>`  <url>\n    <loc>${escapeXml(loc)}</loc>\n    <lastmod>${now}</lastmod>\n    <changefreq>weekly</changefreq>\n    <priority>${loc===origin+'/'?'1.0':'0.7'}</priority>\n  </url>`).join('\n')}\n</urlset>\n`;return new Response(body,{headers:{'Content-Type':'application/xml; charset=utf-8','Cache-Control':'public, max-age=180'}});}catch(e){return new Response('Sitemap error: '+String(e?.message||e),{status:500,headers:{'Content-Type':'text/plain; charset=utf-8'}});}}
function escapeXml(s){return String(s||'').replace(/[<>&'"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':'&quot;'}[c]));}
